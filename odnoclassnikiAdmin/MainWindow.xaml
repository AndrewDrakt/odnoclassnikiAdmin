<Window x:Class="odnoclassnikiAdmin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:odnoclassnikiAdmin"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="40"> </RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock x:Name="text" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="Станция слежения за переписками (абсолютно законно)" VerticalAlignment="Stretch" FontSize="36" />
        <ListBox x:Name="msgLB" Grid.Row="1"/>
        <Button x:Name="save" Content="Сохранить лог" HorizontalAlignment="Stretch" Grid.Row="2" VerticalAlignment="Stretch" Background="Red" Foreground="White" FontSize="24" Click="save_Click"/>

    </Grid>
</Window>
